# Old AIze archive note

This directory keeps a partial old AIze source snapshot for reference.

Sensitive runtime access files were intentionally removed from this archive so
the public repository does not carry old local access cache or related
implementation details.

Behavior changed: Entrance chat/status rendering now suppresses legacy parallel or WorkerAgent-checking claims only when they are not backed by runtime worker state. If a runtime event explicitly carries an active worker service, the visible status can keep that evidence-backed WorkerAgent state instead of hiding it as old optimistic banner text.

Files touched:
- `src/runtime/html_renderer.py`
- `src/runtime/http_handler.py`
- `tests/test_entrance_page.py`

Verification run:
- `PYTHONPATH=./src python3 -m unittest tests.test_entrance_page -v`
- `PYTHONPATH=./src python3 -m unittest tests.test_entrance_page.EntrancePageTests.test_entrance_immediate_ack_does_not_claim_agent_activity_without_state tests.test_entrance_page.EntrancePageTests.test_entrance_legacy_optimistic_ack_is_sanitized_for_display tests.test_entrance_page.EntrancePageTests.test_entrance_delegated_ack_preserves_explicit_route_status tests.test_entrance_page.EntrancePageTests.test_communication_chat_noise_filters_protocol_chatter -v`
- Headless Chrome render check using `.temp/entrance-status-banner-browser.html`: confirmed visible `Goal Active`, `Goal In Progress`, `Executing`, and `All Clear` badges; confirmed unbacked `WorkerAgent is checking in parallel.` text is absent from visible DOM while a runtime-backed worker-state message remains visible.
- Live Headless Chrome check against `https://127.0.0.1:64123/units/entrance?session_id=31df08e901d85c32` after restart: after submit, `has_parallel_claim=false`, `has_misleading=false`, status was `Input sent. Waiting for Entrance updates...`, and chat did not show the old unbacked claim.
- Live Headless Chrome runtime-state injection check against the same page: visible badges were `Goal Active`, `Goal In Progress`, `Executing`, and `All Clear`; unbacked WorkerAgent-checking text was hidden, while the runtime-backed worker-state message remained visible.
- `PYTHONPATH=.:./src python3 .temp/entrance_status_cdp_verify.py`: live Headless Chrome returned `ok=true`, `bodyHasOldClaim=false`, `nonUserOldClaim=false`, status `Input sent. Waiting for Entrance updates...`.
- `/usr/bin/google-chrome --headless=new --disable-gpu --no-sandbox --disable-dev-shm-usage --dump-dom file://$PWD/.temp/entrance-status-banner-browser.html`: confirmed `Goal Active`, `Goal In Progress`, `Executing`, `All Clear`, and runtime-backed worker text render, while the old parallel claim does not.
- Re-run verification after restart resume: `PYTHONPATH=./src python3 -m unittest tests.test_entrance_page -v` passed 46 tests; live Headless Chrome submit against `https://127.0.0.1:64123/units/entrance?session_id=85575a7134f31d0b` returned `has_misleading=false` and `has_parallel_claim=false` with status `Input sent. Waiting for Entrance updates...`; static Headless Chrome render confirmed `Goal Active`, `Goal In Progress`, `Executing`, and `All Clear` remain visible, the unbacked WorkerAgent-checking row is hidden, and a runtime-backed worker-state row remains visible.
- Re-run after the next restart resume: targeted ack/noise tests passed; live Headless Chrome submit against `https://127.0.0.1:64123/units/entrance?session_id=85575a7134f31d0b` again returned `has_misleading=false` and `has_parallel_claim=false` with status `Input sent. Waiting for Entrance updates...`.
- Re-run after a subsequent restart resume: targeted ack/noise tests passed; live Headless Chrome submit against `https://127.0.0.1:64123/units/entrance?session_id=85575a7134f31d0b` returned `has_misleading=false` and `has_parallel_claim=false` with status `Input sent. Waiting for Entrance updates...`.
- Re-run after another restart resume: targeted ack/noise tests passed; live Headless Chrome submit against `https://127.0.0.1:64123/units/entrance?session_id=85575a7134f31d0b` returned `has_misleading=false` and `has_parallel_claim=false` with status `Input sent. Waiting for Entrance updates...`.
- Final resume verification: focused Entrance acknowledgement/status tests passed; live Chrome DevTools browser check against the Entrance page injected both the legacy optimistic text and a runtime-backed worker-state message. Browser DOM result: `hasOldFullClaim=false`, `hasParallelClaim=false`, `hasRuntimeBackedWorker=true`, with visible badges `Goal Active`, `Goal In Progress`, `Runtime Executing`, and `All Clear`.

Remaining risk: this preserves the Entrance coordination role and keeps goal/runtime badges driven by existing state events. Older persisted free-text claims without attached runtime worker fields remain hidden from Entrance-visible chat rows.
